interface:
  display_name: "Root Cause Investigation"
  short_description: "Structured debugging before fixing"
  default_prompt: "Use $root-cause to find the root cause before changing code."
policy:
  allow_implicit_invocation: false
