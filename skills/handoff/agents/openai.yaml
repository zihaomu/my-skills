interface:
  display_name: "Context Handoff"
  short_description: "Save and restore compact project context"
  default_prompt: "Use $handoff to save the current project state for the next session."
policy:
  allow_implicit_invocation: false
